<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="servicefactory.koreanhistory">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label=""
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.study.StudyMajorCategoryListActivity" />
        <activity android:name=".activity.study.StudyMinorCategoryListActivity" />
        <activity android:name=".activity.quiz.QuizActivity" />
        <activity android:name=".activity.quiz.QuizListActivity" />
        <activity android:name=".activity.review.ReviewListActivity" />
        <activity android:name=".activity.review.WrongAnswerListActivity" />
        <activity android:name=".activity.quiz.QuizResultActivity" />
    </application>

</manifest>