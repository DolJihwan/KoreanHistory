<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="servicefactory.koreanhistory">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label=""
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.PeriodChoiceActivity" />
        <activity android:name=".activity.QuizActivity" />
        <activity android:name=".activity.QuizListActivity" />
        <activity android:name=".activity.WrongAnswerListActivit" />
        <activity android:name=".activity.WrongAnswerListActivity" />
        <activity android:name=".activity.QuizResultActivity" />
    </application>

</manifest>